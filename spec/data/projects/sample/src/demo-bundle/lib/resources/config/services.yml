parameters:

    # demo.bundle.service.class: demo-bundle:service/DemoService

services:

    demo.simple.upload.service:
        constructor: "demo-bundle:upload/SimpleUploadService"
        arguments: ["@service_container"]

    demo.resumable.upload.service:
        constructor: "demo-bundle:upload/ResumableUploadService"
        arguments: ["@service_container"]
