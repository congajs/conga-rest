rest:

    # # the serializer to use
    # serializer: conga-rest:rest/serializer/JsonApiRestSerializer
    #
    # # the pagination builder to use (using page based)
    # pagination: conga-rest:rest/pagination/OffsetBasedPagination
    #
    #
    #
    # # the linking root url
    # linking.root.url: "http://localhost:3000/api"
    #
    #

    # the specification class to use
    specification: conga-rest:specification/json-api/JsonApiRestSpecification

    # the attribute handling: camel, snake, hyphen
    attribute.inflection: hyphen

    # the paths to the models
    model:
        paths:
            # - demo-bundle:model
            # - other-bundle:model
